# Setup and Scripts

## Installation
Install dependencies from the root directory:
```bash
npm install

# Start the default portal-backend
npm run start

# Start in watch mode
npm run start:dev

# Start specific services (example)
nest start auth-service
nest start gym-service

### 4. `docs/api-contracts.md`
This file documents the Data Transfer Objects (DTOs) used for API communication.

